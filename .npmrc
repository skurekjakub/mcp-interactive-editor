# Refuse an install on a Node the toolchain does not support, rather than
# producing a tree that works until it does not. jsdom, undici and whatwg-url
# all require newer than the version this repo used to pin, and npm ignores
# `engines` without this.
engine-strict=true

node_modules/
dist/
*.log
.DS_Store
.vite/
coverage/
*.mcpb

# bundle/ is a distribution artifact and IS committed — the Claude Code plugin
# installs by git clone with no build step, so it has to be in the tree.
.eslintcache
